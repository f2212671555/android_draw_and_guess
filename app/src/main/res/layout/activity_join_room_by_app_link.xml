<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFBF00"
    android:paddingLeft="10dp"
    android:paddingTop="10dp"
    android:paddingRight="10dp">

    <TextView
        android:id="@+id/tv_room_name_app_link"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_above="@+id/et_user_name_app_link"
        android:layout_marginBottom="80dp"
        android:text="roomName"
        android:textSize="60dp"/>

    <Button
        android:id="@+id/btn_join_app_link"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:layout_centerVertical="true"
        android:layout_marginBottom="150dp"
        android:background="@drawable/bt_main"
        android:text="JOIN"
        android:textColor="@android:color/white"
        android:textSize="40dp" />

    <EditText
        android:id="@+id/et_user_name_app_link"
        android:layout_width="250dp"
        android:layout_height="100dp"
        android:layout_above="@+id/btn_join_app_link"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp"
        android:background="@drawable/room_list_background_selector"
        android:gravity="center_vertical|center_horizontal"
        android:hint="你的名子"
        android:textAlignment="center"
        android:textSize="40dp" />

</RelativeLayout>